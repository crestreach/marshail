---
name: marshal-load-maintain
description: Loads MARSHAL, the AI-assisted software development lifecycle defined in this repository, into the agent's context for working on the process itself (extending, modifying, or polishing the spec). Use at the start of a fresh session on the Marshal repo, or when the user asks to load, onboard, or brief the agent on the Marshal process before making changes to it.
---

# Marshal Load (Maintain)

Read [`marshal.md`](../../../marshal.md), which contains the full description of MARSHAL — an AI-assisted software development lifecycle. Analyze it and learn it. In this session, we will be working on extending, modifying, and polishing this process.

Also read [`AGENTS.md`](../../../AGENTS.md) for working rules in this repo (e.g. `marshal.md` is the single source of truth; do exactly what the user asks; ask before extending).

After reading, briefly confirm you've loaded the process and are ready to work on it. Don't output the process description. Just confirm that you understand it.
